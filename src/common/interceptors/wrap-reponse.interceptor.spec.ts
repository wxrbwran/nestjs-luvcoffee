import { WrapReponseInterceptor } from './wrap-reponse.interceptor';

describe('WrapReponseInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapReponseInterceptor()).toBeDefined();
  });
});
